supported.extensions=jpg,jpeg,png,bmp,xls,xlsx,pdf,txt
supported.queryParameters=userId,documentName,fromLanguage,toLanguage,applyXlsRules

s3.bucketName=${BUCKET_NAME:#{null}}
textExtract.functionName=${TEXT_EXTRACT_LAMBDA:#{null}}

translate.option=${TRANSLATE_OPTION:#{AWS}}

spring.datasource.url=${DB_URL:#{null}}
spring.datasource.username=${DB_USER_NAME:#{null}}
spring.datasource.password=${DB_PASSWORD:#{null}}

spring.flyway.enabled=false
